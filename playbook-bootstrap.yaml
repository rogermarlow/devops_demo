---
 # Play 1
 - name: Get VirtualBox and Vagrant
   hosts: localhost
   connection: local

   vars:
    homebrew_cask_appdir: /Applications
    homebrew_cask_apps:
      - virtualbox
      - vagrant
   roles:
      - role: elliotweiser.osx-command-line-tools
      - role: geerlingguy.homebrew

 # Play 2
 # - name:
